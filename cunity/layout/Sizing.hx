package me.cunity.layout;

/**
 * ...
 * @author axel@cunity.me
 */
enum Sizing 
{
	NATIVE;
	/*FIXED(p:Float, p:Float);
	HFIXED(p:Float);
	VFIXED(p:Float);
	PERCENT(p:Float, p:Float);
	HPERCENT(p:Float);
	VPERCENT(p:Float);*/
	FILL;
	HFILL;
	VFILL;
}